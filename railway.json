{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bun run --cwd packages/database db:generate && bun run --cwd packages/server build"
  },
  "deploy": {
    "startCommand": "bun run --cwd packages/database db:push && bun --cwd packages/server dist/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
